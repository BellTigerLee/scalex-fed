# federation/karmada/overrides

Store federation-wide Karmada override policy resources here.

Use project-local override files for app-specific behavior. This directory is
for shared override conventions that should apply across project boundaries.
